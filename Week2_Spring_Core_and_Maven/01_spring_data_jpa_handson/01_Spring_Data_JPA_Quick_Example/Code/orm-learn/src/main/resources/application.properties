# Database Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/ormlearn?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root

# ⚠️ YAHA APNA REAL PASSWORD DALO (Agar root hai to root, nahi to jo bhi tumhara hai)
spring.datasource.password=12345678

# Hibernate settings
spring.jpa.hibernate.ddl-auto=validate

# ✅ FIX: Hibernate 6+ ke liye generic MySQL dialect use karo
